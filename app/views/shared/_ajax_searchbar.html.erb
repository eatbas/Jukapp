<%= form_tag ajax_search_videos_path, method: :get, class: "form-inline", remote: true, id: "search" do %>
  <div class="form-group">
    <%= text_field_tag "query", params[:query], class: "form-control", placeholder: "Search on YouTube", :autofocus => "autofocus" %>
  </div>
  <%= button_tag :class => "btn btn-default", type: :submit do %>
    <span class="glyphicon glyphicon-search"></span>
  <% end %>
<% end %>

<div id="search-results"></div>

<script>
  $(function() {
    $("#search").on("ajax:success", function(e, data, status, xhr) {
      $("#search-results").html(data)
    }).on("ajax:error", function(e, xhr, status, error) {
      $("#search").append("<p>ERROR</p>");
    });
  });
</script>
